const oauth_config = Object.assign({
  TOKENCREDS : {
      client_id: 'cd_web_app',
      client_secret: 'cd_web_app_secret',
      grant_type: 'client_credentials'
  }
})

export default oauth_config
